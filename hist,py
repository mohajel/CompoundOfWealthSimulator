# sketch PDF from lots of samples of random variables
# plot histogram of samples

import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# get samples from simple_consense_protocol_test.txt
# read data from file
data = np.loadtxt('simple_consense_protocol_test.txt')

# plot data and set x and y labels x = participant number, y = coin value
# plt.xlabel('Participant')
# plt.ylabel('Coin valueEE')

# get only first set of samples
samples = data[:,0]

# print samples 
print(samples)

# divide all samples to 10000
samples = samples/20000

plt.hist(samples, bins=50, density=True, histtype='stepfilled')

 
# plt.hist(samples, bins=100, density=True, )

#connect hists with line and only show line
# plt.hist(samples, bins=100, density=True, histtype='step')


# plot histogram of samples
# plt.hist(samples, bins=100)

# # number of samples
# n = 10000

# # generate samples from random variable
# samples = np.random.normal(size=n)

# # plot histogram of samples
# plt.hist(samples, bins=100)

# # show plot
plt.show()

# save plot as png file
plt.savefig('histogram.png')


